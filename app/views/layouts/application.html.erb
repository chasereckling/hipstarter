<!DOCTYPE html>
<html>
<head>
  <title>Hipstarter</title>
  <link href='http://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>


  <% if flash[:notice] %>
    <div class="flash-wrapper">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
  <% end %>

  <nav class="navbar navbar-default navbar-fixed-bottom">

    <ul class="nav navbar-nav">
      <%= link_to image_tag("/images/camera_icon.png", class: "camera_icon"), root_path %>


      <% if user_signed_in? %>
        <li class="navbar-right">
          <span>
              You are logged in as <i><%= current_user.email %></i>! |
              <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          </span>
        </li>
      <% else %>
        <li class="navbar-right"><%= link_to "Sign In", new_user_session_path %></li>
        <li class="navbar-right"><%= link_to "Signup",  new_user_registration_path %></li>
      <% end %>

    </ul>
    </nav>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
